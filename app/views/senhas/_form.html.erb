
<%= form_with(model: senha, local: true) do |form| %>
  <% if senha.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(senha.errors.count, "error") %> prohibited this senha from being saved:</h2>

      <ul>
      <% senha.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :comanda, style: 'width:500px; height:50px; font-size:50px;' %>
  </div>

  <div class="actions">
    <%= form.submit "Chamar", style: 'width:300px; height:60px; font-size:50px; color:#333; background-color: #B9D2B1;'  %>
  </div>
<% end %>

